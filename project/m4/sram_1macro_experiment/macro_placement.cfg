# Macro placement — single SRAM macro.
# Format: <instance_name> <x_um> <y_um> <orientation>
#
# 1 × sky130_sram_1kbyte_1rw1r_32x256_8 (479.78 × 397.5 µm).
#
# Placed in the lower-left of a 2000×2000 µm die. Macro footprint occupies
# 100..580 µm × 100..497.5 µm. The remaining ~3.5 mm² of core is uniform open
# space for compute logic, which removes every macro-edge ↔ PDN-stripe
# collision mode that fractured the 16-macro run.

u_bank   100.00   200.00  N
